@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@using ThreeHundredRepro

@if (true)
{
    @* <partial name="Test"
             model='@new WarningModal
                 {
                    ModalId = "outstandingReviewPointsWarningModal",
                    ModalTitle = "Export Trial Balance",
                    Message = "There are still some outstanding review points for this job, are you sure you want to export the trial balances?"
                 }' /> *@
}